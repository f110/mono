package: libmpdec3

slices:
  libs:
    essential:
      - libc6_libs
      - libgcc-s1_libs
      - libstdc++6_libs
    contents:
      /usr/lib/*-linux-*/libmpdec.so.3*:
      /usr/lib/*-linux-*/libmpdec++.so.3*:
      /usr/lib/*-linux-*/libmpdec++.so.2*:
      /usr/lib/*-linux-*/libmpdec.so.2*:
